node_modules/
package-lock.json
/.workbuddy
nul
verity/verity_2.7z
build/
# 站长本地工具（严禁提交，仅本地使用）
paykey.htm
adkey.htm
admin/

# 本地测试产物（严禁提交）
test-results/
playwright-report/

# Playwright CLI 残留日志
.playwright-cli/

# 文章编辑器实时预览临时文件
/.md_preview

# Python 缓存 & 备份
__pycache__/
*.pyc
*.bak

# 排除误入的用户目录(系统/软链,禁止入库)
用户的/

# 排除本地备份目录(不入库)
blog_backup_20260822/

# CS 游戏：本地测试副本 & 调试脚本（临时，严禁入库）
CS_test/
CS/debug-*.js

# CS 模型 & ORT 推理运行时：全部直接入库（含 int4 量化模型）。
# 当前模型：onnx-community/Qwen2.5-0.5B-Instruct（q4f16，460MB），放 CS/assets/model/qwen25/。
# 因 GitHub Pages 单文件限 100MB、且不支持 LFS，模型被切成 model.onnx.00~.04 五块（各 <100MB），
# 由 CS/assets/code/chatai-worker.js 在 worker 内拼合后喂给引擎。
# 旧模型 CS/assets/model/gpt2ft/ 已废弃（仅本地残留，未入库）。
