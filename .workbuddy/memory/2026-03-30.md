# 2026-03-30 日志

## 修复 index.html 验证步骤5响应式 Bug（18:54）
- **问题**：step5"你是人吗"有27个身份按钮，内容超出视口高度，"确认身份"按钮被挤到屏幕外无法点击
- **修复内容**：
  1. `.verify-box` 加上 `max-height: 90vh; overflow-y: auto; box-sizing: border-box`，使弹窗内部可滚动
  2. `.verify-overlay` 改为 `align-items: flex-start; overflow-y: auto; padding: 20px 0`，允许整个遮罩层滚动
  3. `@media (max-width:576px)` 追加 `.verify-box { padding: 24px 16px; }` / `.gender-btn { padding: 8px 6px; font-size: 0.78rem; }` / `.verify-title { font-size: 1.4rem; }` 让小屏更紧凑
