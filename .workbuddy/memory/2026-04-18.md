# 2026-04-18 工作日志

## blog/ 文章内联化迁移（完成）
- 将 `css/wz.css` 和 `css/share.css` 内联到 blog/ 下所有依赖这两个文件的 HTML 文件中（`<style>` 标签形式）
- 将 `css/wz/` 目录下各JS文件内联到对应 HTML 文件中（`<script>` 标签形式）
  - 2.html ← wzjsid2.js
  - 7.html、8.html、10.html ← 无JS，仅CSS内联
  - 9.html ← wzjsid9.js
  - 11.html ← wzjsid11.js
  - muban.html ← wzjs模板.js
- 1.html 已在前一次会话中完成内联
- 3.html、4.html、5.html、6.html 原本已是内联形式，无需修改
- 删除 `css/wz.css`、`css/share.css` 对应 link 标签、外部 script src 引用
- 删除 `css/wz/` 目录及其下所有JS文件
- 删除 `css/wz.css` 文件（share.css 保留，未删除）
- 使用 Python 脚本（inline_assets.py，事后已删除）批量处理，Python路径：`C:\Users\nb666\AppData\Local\Programs\Python\Python313\python.exe`

## blog/ 目录重组为子目录结构（完成）
- 在 `blog/` 下创建11个文件夹（1~11）
- 将 `blog/1.html` ~ `blog/11.html` 分别移入对应文件夹并重命名为 `index.html`
  - 例如：`blog/1.html` → `blog/1/index.html`
- 更新 `articles-data.js` 中所有 `fileName` 字段，从 `N.html` 改为 `N/index.html`
- 注意：如果网站其他地方有直接引用 `blog/N.html` 的链接，也需要同步更新

## URL 格式最终确定为 blog/N/（无 index.html）
- `articles-data.js` 中所有 `fileName` 从 `N/index.html` 改为 `N/`
- `index.html` 硬编码链接改为 `./blog/1/`
- `blog/N/index.html` 的 canonical 标签改为 `91vip.xn--32v.ink/blog/N/`
- `blog/11/index.html` 内容中的文章分享链接改为 `blog/11/`
- `blog/11/index.html` 相邻文章导航链接（10.html/9.html/8.html/7.html）改为 `10/`/`9/`/`8/`/`7/`
- `blog/4/index.html` 文章列表和导航链接（1~3.html）改为 `1/`/`2/`/`3/`/`3/`
- `status.html` 文章链接（./1.html ~ ./10.html）全部改为 `./blog/1/` ~ `./blog/10/`，显示文字同步更新
