# 2026-04-06 日志

## 多维度审查 + 全部修复（index.html / adm1n.html）

### P0 修复
- `adm1n.html`：明文密码 `guangeadmin123` 改为 SHA-256 哈希验证（`crypto.subtle.digest`），登录逻辑改为 `async/await`；登录失败计数从 localStorage 改用 sessionStorage 防绕过
- `index.html`：删除 `openAdminPanel()` 中的 `btoa('guangeadmin123_...')` 伪加密行及 `console.log('别进我后台...')` 泄露
- 验证选项：删除 `棍母`、`糖心vlog`、`我已满18`、`我未满18`、`919191919178787878`、`男拉拉`、`女给给`、`2.0棍母`、`唐塔菲`（重复永雏塔菲）等不当选项

### P1 修复
- 导航栏 & mobile-menu：删除所有 `#donation` 死链
- mobile-menu 头部：删除失效 `avatar.png` 引用，改为纯文字
- 抖音 section：重复 `id="bilibili-space"` 改为 `id="douyin-space"`，同步更新翻译数组中所有选择器
- IP 验证步骤：添加隐私说明（本站仅在验证期间读取IP，不做存储），外国IP拒绝界面补充VPN提示
- noscript 邮箱：`3627742771@qq.com` 改为 `3627742771 [at] qq.com` 防爬虫采集
- timeline 渲染：新增 `escapeHtml()` 函数，`item.title`/`item.desc`/`item.date` 均经过 HTML 转义再拼入 innerHTML，防 XSS

### P2 修复
- 删除 Bot 检测中 `console.log('[SEO Bot Detection]...')` 调试行
- 调试面板 `addLog()` 的 `console.log` 改为仅在 `debugMode === true` 时输出
