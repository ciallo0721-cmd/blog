# 2026-05-12 日志

## sitemap.xml 定时更新（19:30）
- 扫描 blog/1~16+grayscale-test、7个静态页面、6个游戏页面
- 修正所有 lastmod 为文件实际修改时间（远端自动脚本将 lastmod 全设 2026-05-12 有误）
- 遇到远端分叉（远端 caf3be9 与本地冲突），用 merge 方式解决，以本地实际日期为准
- 推送成功：6f55379

## code 标签黑色遮罩效果
- 修改了 `blog/muban/muban.html` 及 `blog/1~16/index.html`（共16篇，grayscale-test也包含）的 `.article-content code` 样式
- 效果：默认显示纯黑色填充块遮住代码，鼠标悬停时遮罩消失显示代码文字
- 实现方式：`color: transparent` + `::after` 伪元素黑色填充，`:hover` 时恢复颜色并隐藏遮罩
- blog/15 本身无此样式，未修改

## 创建本地开发服务器 local_server.py（19:43）
- 端口 9000，自动打开浏览器
- 通过注入 JS 跳过 Cloudflare Turnstile 验证流程
- 原理：在 `</head>` 前注入脚本，自动设置 sessionStorage.verify_passed='true' 和 auth_passed='true'，隐藏验证 overlay，添加 auth-passed 类，MutationObserver 持续监控防止验证层重新出现
- Turnstile sitekey 在前端：`0x4AAAAAACrQ7Ek_dIwdbxML`
