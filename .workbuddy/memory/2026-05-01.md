# 2026-05-01 工作日志

## Git 历史清空
- 使用孤儿分支方式彻底清空 Git 历史记录
- 当前所有文件打包为新的初始提交：`initial commit - clean history backup (2026-05-01)`（commit: d7c8551）
- 之后的修复提交：`fix: 修复推荐文章/上一篇下一篇链接路径 blog/id/id 双重叠加 bug`（commit: 3461e6d）
- ⚠️ 远端需要用户手动执行：`git push origin main --force`（网络问题未成功推送）

## Bug 修复：推荐文章路径双重叠加
- **问题**：`articles-data.js` 中所有文章 `fileName` 为相对路径（如 `"1/"`），在 `blog/1/index.html` 页面中使用时会叠加成 `blog/1/1/`
- **修复**：将所有 14 篇文章的 `fileName` 改为绝对路径（如 `"/blog/1/"`）
- **影响范围**：推荐文章卡片链接 + 上一篇/下一篇导航链接同时修复

## 主页新增"网站运行状态"区块（commit: 4c4d2c）
- **位置**：导航栏下方、动画分隔线上方
- **显示内容**：
  - 🟢 网站状态（正常/故障，从 `dynamic-data.js` 的 `siteStatus` 字段读取）
  - 🕐 当前时间（北京时间，每秒实时刷新）
  - 📅 已运行 N 天（从 2025-10-20 起算，JS 实时计算）
- **变更文件**：
  - `index.html`：新增 CSS 样式 + HTML 结构 + JS 实时更新逻辑
  - `.github/workflows/dynamic-update.yml`：修改 `SITE_START` 为 `2025-10-20`
  - `dynamic-data.js`：新增 `siteStatus: "正常"` 字段，修正起始日期和天数

## 技术细节
- `dynamic-data.js` 新增 `siteStats.siteStatus` 字段，可在 workflow 或手动修改
- `index.html` 的 JS 用 `setInterval(updateCurrentTime, 1000)` 实现每秒刷新
- 网站状态绿灯/红灯有 pulse 动画效果
