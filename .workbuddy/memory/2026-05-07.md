# 2026-05-07 日志

## 公告弹窗（index.html）
- 在 index.html `</body>` 前新增公告弹窗组件
- 特性：验证通过后 800ms 弹出，支持"今天不再显示"（localStorage 按日期存储），点遮罩/×/按钮均可关闭
- 公告正文内容在 `#announcementContent` div 内修改

## 删除 GitHub Actions 工作流
- 删除了 4 个工作流文件：
  - dynamic-update.yml（每6小时自动更新伪动态数据）
  - scheduled-publish.yml（每30分钟检查定时发布）
  - jekyll-docker.yml
  - jekyll-gh-pages.yml
- .github/workflows/ 目录现在为空
