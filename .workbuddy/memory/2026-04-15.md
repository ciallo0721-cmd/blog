# 2026-04-15 工作日志

## 文章目录迁移完成

### 完成的任务
1. 创建 `blog/` 目录
2. 将所有文章（1.html ~ 11.html）移动到 `blog/` 目录
3. 修改所有引用文章的页面，更新链接为 `blog/` 路径：
   - `index.html` - 首页文章链接
   - `wz.html` - 文章列表页
   - `css/indexjs.js` - 公共 JS 库

### 修改的文件
- `index.html`：文章链接从 `article.fileName` 改为 `'blog/' + article.fileName`
- `wz.html`：文章链接从 `article.fileName` 改为 `'blog/' + article.fileName`
- `css/indexjs.js`：文章链接从 `safeFileName` 改为 `'blog/' + safeFileName`

### 后续建议
- 更新 sitemap.xml 添加 blog/ 路径
- 检查 robots.txt 是否正确允许爬虫访问 blog/
- 确认 Google Search Console 抓取新路径